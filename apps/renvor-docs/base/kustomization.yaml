apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - networkpolicy.yaml
  - resourcequota.yaml
  - limitrange.yaml

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: renvor
